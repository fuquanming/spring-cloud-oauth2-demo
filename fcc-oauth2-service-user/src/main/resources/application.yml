server:
  port: 60011
  
spring:
    application:
        name: user    

logging.level.org.springframework.security: DEBUG

# 添加注册中心
eureka:
    client:
        service-url:
            defaultZone: http://localhost:20000/eureka/           
        
        